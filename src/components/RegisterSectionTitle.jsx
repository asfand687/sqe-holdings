import React from 'react'

const RegisterSectionTitle = ({ className, text }) => {
  return (
    <h2 className={`text-5xl font-medium text-center py-4 ${className}`}>{text}</h2>
  )
}

export default RegisterSectionTitle